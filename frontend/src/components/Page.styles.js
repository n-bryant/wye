const styles = theme => ({
  root: {
    position: "relative",
    height: "100%"
  },
  innerContent: {
    height: "calc(100% - 56px)",
    backgroundColor: theme.palette.common.black,
    overflow: "auto"
  }
});
export default styles;
