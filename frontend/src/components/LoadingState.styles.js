const styles = theme => ({
  root: {
    height: "100%",
    width: "100%",
    display: "flex",
    justifyContent: "center",
    alignItems: "center"
  },
  progressIndicator: {
    color: theme.palette.common.blue6
  }
});
export default styles;
