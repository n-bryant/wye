const styles = theme => ({
  root: {},
  label: {
    color: theme.palette.common.blue6
  },
  switch: {
    color: theme.palette.common.white
  },
  switchChecked: {
    color: `${theme.palette.common.blue6} !important`
  }
});
export default styles;
