const styles = theme => ({
  root: {},
  itemsContainer: {
    marginTop: theme.spacing(0.5),
    height: "246px",
    justifyContent: "center"
  },
  item: {
    height: "100%"
  },
  quickLinkContainer: {
    display: "flex",
    justifyContent: "flex-end"
  }
});
export default styles;
