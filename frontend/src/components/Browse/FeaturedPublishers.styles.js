const styles = theme => ({
  root: {},
  itemsContainer: {
    marginTop: theme.spacing(0.5),
    height: "246px",
    justifyContent: "center"
  },
  item: {
    height: "100%"
  }
});
export default styles;
