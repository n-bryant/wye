const styles = theme => ({
  root: {
    background: "rgba(0,0,0,.5)",
    padding: "12px",
    display: "flex",
    justifyContent: "space-evenly"
  },
  infoContainer: {
    padding: "1rem"
  }
});
export default styles;
